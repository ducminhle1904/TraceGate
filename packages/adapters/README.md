# @tracegate/adapters

Planned package area for framework adapters.

Initial targets are OpenAI Agents SDK for JavaScript and LangGraph JS. Adapters should stay thin and preserve the framework-neutral contracts from `@tracegate/core`.
