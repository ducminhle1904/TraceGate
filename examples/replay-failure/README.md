# Replay Failure Example

Planned example showing how a production or local trace becomes a deterministic replay fixture.

This example will be implemented after trace fixtures and `tracegate replay` exist.
