{
  "name": "tracegate-example-compatibility-nested-agent",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test:tsx": "tsx src/agent.ts",
    "test:matrix": "pnpm --filter @tracegate/cli build && pnpm exec tracegate doctor && pnpm exec tracegate test --json",
    "test:all": "pnpm test:tsx && pnpm test:matrix"
  },
  "dependencies": {
    "@tracegate/cli": "workspace:*",
    "@tracegate/core": "workspace:*",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
