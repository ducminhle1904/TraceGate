# Plain Function Tools Template

Shows how to wrap an existing function registry with TraceGate runtime gates.

```bash
pnpm --filter tracegate-template-plain-functions check
```

The template demonstrates contract creation from a registry, observe mode, shadow comparison,
validation-only enforcement, JSONL trace capture, and `tracegate replay-runtime`.
