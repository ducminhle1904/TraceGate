# LangGraph/LangChain Template

Shows a local LangChain structured tool plus TraceGate runtime-gate rollout probes.

```bash
pnpm --filter tracegate-template-langgraph-langchain check
```

No model or API key is required; the template invokes the structured tool directly.
