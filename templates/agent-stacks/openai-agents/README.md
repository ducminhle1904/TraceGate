# OpenAI Agents SDK Template

Shows a local OpenAI Agents function tool plus TraceGate runtime-gate rollout probes.

```bash
pnpm --filter tracegate-template-openai-agents check
```

No model or API key is required; the template invokes the SDK tool directly.
