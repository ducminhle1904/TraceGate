# Vercel AI SDK Template

Shows TraceGate-wrapped AI SDK `tool()` definitions without calling a model.

```bash
pnpm --filter tracegate-template-vercel-ai-sdk check
```

The template invokes tool `execute()` functions directly for local CI smoke coverage.
